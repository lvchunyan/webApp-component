<!--page/API/index.wxml-->
<view>
  <view class='index-hd'>
    <image class="index-logo" src="resources/kind/logo.png"></image>
    <view class="index-desc">以下将演示小程序接口能力，具体属性参数详见小程序开发文档。</view>
  </view>
  <view class='index-bd'>
    <block wx:for="{{list}}" wx:key="item.id">
      <view class='kind-list'>
        <view id="{{item.id}}" class="kind-list-item {{item.open ? 'kind-list-item-show' : ''}}" bindtap='kindToggle'>
          <view class="kind-list-text">{{item.name}}</view>
          <image class="kind-list-img" src="resources/kind/{{item.id}}.png"></image>
        </view>
        <view class="navigator-box {{item.open ? 'navigator-box-show' : ''}}">
          <view wx:for="{{item.pages}}" wx:for-item="page" wx:key="*item">
            <navigator url="pages/{{page.url}}" class='navigator'>
              <view class="navigator-text">{{page.zh}}</view>
              <view class="navigator-arrow"></view>
            </navigator>
          </view>
        </view>
      </view>
    </block>
  </view>
</view>
